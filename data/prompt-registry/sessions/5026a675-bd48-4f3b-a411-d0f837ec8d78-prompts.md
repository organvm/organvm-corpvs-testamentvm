# Session Prompts: 2026-06-11

**Session ID:** `5026a675-bd48-4f3b-a411-d0f837ec8d78`
**Duration:** ~11 min
**Working directory:** `/Users/4jp`
**Prompts:** 28 human messages

---

### P1 — 2026-06-11 13:13:17

<scheduled-task name="daily-dependabot-merge" file="/Users/4jp/.claude/scheduled-tasks/daily-dependabot-merge/SKILL.md">
This is an automated run of a scheduled task. The user is not present to answer questions. For implementation details, execute autonomously without asking clarifying questions — make reasonable choices and note them in your output. "write" actions (e.g. MCP tools that send, post, create, update, or delete), only take them if the task file asks for that specific action. When in doubt, producing a report of what you found is the correct output.

## Version: v1.1.0
## Status format: `<N>-deps-tier2=<X>-tier3=<Y>-merges=<M>-skipped=<S>` (e.g., `58-deps-tier2=21-tier3=34-merges=5-skipped=53`)

## Metadata
- **Owner:** conductor (`4444J99` / `4444jPPP`); merges occur via the conductor's `gh` auth
- **Class:** I (local-token; migrates to Class-(III) with `daily-pr-management` in doc 19 Phase 4)
- **Cadence:** daily (09:11 EDT — 10 min after `daily-pr-management` so the per-repo state stabilizes)
- **Author scope:** `dependabot[bot]` ONLY. Conductor PRs go through `daily-pr-management`; Jules PRs go through `IRF-OPS-082` one-time triage.
- **IRF row:** IRF-OPS-083 (registration + first-fire shakeout)
- **Depends on:** `gh` CLI auth, `${REGISTRY_V2}` for tier classification, `~/.local/bin/dependabot-tier-classify` (canonical Phase-B resolver), `~/.local/bin/scheduled-task-audit-bookend`
- **Audit log path:** `~/.claude/scheduled-tasks/audit/YYYY-MM-DD.log`
- **Conformance:** docs/standards/24 v1.1.0; routes through docs/standards/17 §10 (this routine is registered as a Section-10 peer below)
- **Last semver bump:** 2026-06-10 — v1.1.0: Phase B moved from prose to the canonical deterministic resolver `dependabot-tier-classify` (fixes the `a-organvm` redirect-alias lookup miss that dropped ~43/57 PRs to unknown→Tier 1, and the prose-divergence behind the two opposite 2026-06-10 fires); added `protected` report-only guard; cap aligned to §10 (8→3). Prior: 2026-05-28 — initial v1.0.0 (response to 2026-05-27 forensic post-mortem of `daily-pr-management`, which surfaced 58 dependabot PRs invisible to the conductor-scoped routine)

## Audit log (start — invoke FIRST)
Run: `~/.local/bin/scheduled-task-audit-bookend daily-dependabot-merge start`
Per docs/standards/18 §9, every Class-(I) fire emits start + end bookends regardless of writes.

Operate under `~/Code/organvm/organvm-corpvs-testamentvm/docs/standards/17-branch-governance.md` Sections 8 + 10. **Read it first.** If unreadable, abort to report-only mode (Phase E).

## Pre-flight (all must pass — any fail = abort writes, run Phase E only)
- Standards doc 17 readable
- Audit log path writable
- `gh auth status` returns authenticated as `4444J99`
- `repo-registry.json` readable at `~/Code/organvm/organvm-corpvs-testamentvm/repo-registry.json`
- The `dependabot[bot]` identity exists in at least one open PR in the conductor orgs (sanity check — if zero, the routine no-ops cleanly)

## Procedure

### Phase A: enumerate (read-only, ONCE)
For each conductor-owned org/user (`4444J99`, `a-organvm`, `meta-organvm`, `organvm-i-theoria` through `-vii-kerygma`):

```
gh search prs --owner=<org> --state=open \
  --author=app/dependabot \
  --json url,number,title,isDraft,author,repository,updatedAt,commentsCount \
  --limit 1000
```

Notes:
- `--author=app/dependabot` is the canonical GitHub-search selector for the dependabot GitHub App. Verify match by checking `.author.login == "dependabot[bot]"` AND `.author.type == "Bot"` post-fetch (defense-in-depth against spoofed names).
- `--limit 1000` is required — `daily-pr-management` v1.0.1 documents the 30-default truncation precedent.

### Phase B: tier-classify (read-only, ONCE — via canonical deterministic script)
**Classification is NOT done in prose.** Pipe every PR slug enumerated in Phase A
through the single canonical resolver so every fire agrees byte-for-byte (this
removes the prose-divergence that produced opposite tier distributions across the
two 2026-06-10 fires):

```
printf '%s\n' "${SLUGS[@]}" | ~/.local/bin/dependabot-tier-classify --stdin
```

It emits one JSON object per slug: `{slug, tier, resolution, matched_org,
matched_name, promotion_status, repo_tier, protected, reason}`. Read `.tier`,
`.resolution`, and `.protected` per PR. If the script exits non-zero (registry
unreadable), **abort writes → Phase E** (never guess tiers without the source of
truth). Run `~/.local/bin/dependabot-tier-classify --self-test` in pre-flight.

The script implements `docs/standards/17` §8 tiering faithfully, PLUS the
**`a-organvm` redirect-alias resolution** the prose lacked: organ-org repos were
transferred to the umbrella org `a-organvm/*` (organ-org slugs are now GitHub
redirect aliases — verified 2026-06-10), so a PR on `a-organvm/<name>` resolves to
its organ-org registry row by unique name. Tier rules it applies (first match wins):
- **Tier 1**: `promotion_status == "GRADUATED"` AND `tier ∈ {infrastructure, flagship, sovereign}` — **NEVER merged** by this routine
- **Tier 2**: `promotion_status ∈ {PUBLIC_PROCESS, CANDIDATE}` OR `tier == "standard"`
- **Tier 3**: `promotion_status ∈ {LOCAL, ARCHIVED}` OR `owner == 4444J99`
- **Unknown OR ambiguous (name collides across organs, e.g. `.github`, `hokage-chess`)** → **Tier 1** (strictly Tier-1-preserving guard — resolution only ever raises caution, never lowers it)

`protected == true` flags any repo whose registry `tier ∈ {flagship, infrastructure,
sovereign}` regardless of promotion_status. Phase C/D treat `protected` as
report-only (see eligibility) — a structurally load-bearing repo is never
auto-merged even when it classifies Tier 2 (e.g. `4444J99/portfolio`, a flagship in
PUBLIC_PROCESS). This is the literal enforcement of the "never auto-merge into a
protected flagship" guard.

### Phase C: per-PR state-fetch + eligibility filter (read-only)
For each candidate (Tier 2 or Tier 3, non-draft, **AND `protected == false`** — a
`protected` repo is report-only per Phase B), fetch detail:

```
gh pr view <n> --repo <slug> \
  --json url,number,title,isDraft,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup,commits,headRefName,baseRefName,updatedAt
```

Eligibility — ALL must hold:
- Author confirmed `dependabot[bot]` (Bot type)
- `tier ∈ {2, 3}` AND `protected == false` (Phase B classifier output)
- `isDraft == false`
- `mergeStateStatus == "CLEAN"` (no conflicts, all required checks passed, no review blocks)
- `reviewDecision ∈ {APPROVED, null}` — `null` means no review required (typical for dep bumps on conductor repos)
- All entries in `statusCheckRollup[]` have `conclusion ∈ {SUCCESS, NEUTRAL, SKIPPED}` — any FAILURE / ERROR / TIMED_OUT / CANCELLED disqualifies
- `commits[]` length ≥ 1 AND the latest commit's `committedDate` is ≥ 24h old (soak window — guards against transient green CI that flips red on retry)
- Title matches dependabot conventional-commit pattern: starts with `chore(deps):`, `chore(deps-dev):`, or `build(deps):` (defense against spoofed bot PRs)

### Phase D: SAFE WRITES — squash-merge eligible PRs (Tier 2/3, non-protected)
**Cap: max 3 merges per fire** — conforms to `docs/standards/17` §10 "Per-run caps"
(Max 3 auto-merges performed per task run). This routine has NO named cap-raise
exemption; if queue drain at 3/fire proves too slow, the conductor amends §10 with a
named higher dependabot cap + rationale (a re-authorization event), and this line is
updated to match. Execute oldest-first (`sort by updatedAt asc`), with 30s stagger
between merges (avoids GitHub Apps rate-limit clustering). Surplus eligible PRs are
reported under Phase E(b), not merged.

For each eligible PR:
```
gh pr merge <PR-URL> --squash --delete-branch
```

Per-action audit row:
```
~/.local/bin/scheduled-task-audit-bookend daily-dependabot-merge merge "<slug>#<n>-tier<t>"
```

No comment is posted — dependabot's own commit message + the merge event suffices. (Section 10 hard-NEVER: no substantive comments.)

### Phase E: READ-ONLY REPORT
Emit a structured Markdown report:
- **(a) Merged this fire:** PR list with tier + age
- **(b) Eligible but capped:** PRs that passed eligibility but were dropped by the 8-merge cap — surface so the conductor sees the queue depth
- **(c) Skipped (CI red):** dependabot PRs with failing checks — these often indicate a real upstream regression and are conductor work to investigate
- **(d) Skipped (Tier 1):** never auto-merged; manual conductor review required
- **(e) Stale > 14d:** dependabot PRs older than 14 days — surface for explicit close/rebase decision

## Failure semantics
- Single failure (one merge errors): log to audit, continue to next eligible PR
- 3 failures in Phase D: abort remaining merges this fire; log `phase-d-abort` audit row
- State-mismatch detected at merge time (PR went un-CLEAN between Phase C fetch and Phase D merge): skip + log `state-mismatch` audit row
- Hard wall-clock cap: 10 minutes total. Exceeded → emit `wall-clock-cap` audit row and abort

## What this task NEVER does (Section 10 hard-NEVERs)
- **Tier 1 merges** — even dependabot patch-bumps need eyes on infrastructure / flagship / sovereign repos
- **`protected == true` merges** — any repo whose registry `tier ∈ {flagship, infrastructure, sovereign}` is report-only regardless of `promotion_status` or computed tier (closes the "auto-merge into a protected flagship" vector even for non-GRADUATED flagships)
- **Classifying without the canonical resolver** — Phase B is `dependabot-tier-classify` ONLY; never hand-classify in prose (the divergence that caused the two 2026-06-10 fires to disagree)
- **Code amendments** — never edit a dependabot PR's diff; if conflict, dependabot rebases itself
- **Substantive comments** — merge event is the entire ceremony; no `/rebase`, no `@dependabot squash and merge`, no narrative replies
- **Force pushes / direct main pushes** — squash-merge via PR only
- **Action on Tier 1 PRs even when CLEAN + APPROVED** — conductor must explicitly merge those
- **Action on draft dependabot PRs** — dependabot rarely opens drafts; if seen, treat as anomaly + skip
- **Action on non-dependabot PRs** even if title matches `chore(deps):` — author-identity gate is enforced post-fetch

## Audit log (end — invoke LAST, after the report)
Run: `~/.local/bin/scheduled-task-audit-bookend daily-dependabot-merge end <status>`
where `<status>` follows format `<N>-deps-tier2=<X>-tier3=<Y>-merges=<M>-skipped=<S>`. Use hyphens; no whitespace.

## Section 10 contract — registration block (to be appended to docs/standards/17 §10)

This routine MUST appear in `docs/standards/17-branch-governance.md` §10 as a registered peer to `daily-pr-management`. Block text for that doc:

> **`daily-dependabot-merge`** — Class-(I) peer to `daily-pr-management`. Author scope: `dependabot[bot]` only. Phase-B classification is the canonical deterministic resolver `~/.local/bin/dependabot-tier-classify` (resolves `a-organvm` redirect aliases; ambiguous/unknown → Tier 1). Action: squash-merge eligible PRs (Tier 2/3 AND `protected == false`, CLEAN, all CI green, ≥24h soak, title matches `chore(deps)|build(deps)`). Caps: **3 merges/fire** (matches §10 Per-run caps), 30s stagger, 10min wall-clock. Hard NEVERs: Tier 1 merges, `protected` merges, code amendments, comments, force pushes. Conductor authorization is durable as long as the spec at `~/.claude/scheduled-tasks/daily-dependabot-merge/SKILL.md` matches this contract block; any spec change is a re-authorization event.

A future `weekly-routine-conformance-witness` fire will diff this block against the live spec — keep them aligned.
</scheduled-task>

**Actions taken:**
- Bash: `~/.local/bin/scheduled-task-audit-bookend daily-dependabot-merge start 2>&1; echo "---EXIT:$?---"`
- Bash: `echo "=== gh auth status ==="; gh auth status 2>&1 | head -20
echo "=== standards doc 17 readable ==="; test -r ~/Code/o...`
- Bash: `echo "=== classifier self-test ==="; ~/.local/bin/dependabot-tier-classify --self-test 2>&1 | tail -20; echo "---EXIT:${...`
- Bash: `sed -n '/^## *8/,/^## *9/p' ~/Code/organvm/organvm-corpvs-testamentvm/docs/standards/17-branch-governance.md 2>/dev/null...`
- Bash: `ORGS=(4444J99 a-organvm meta-organvm organvm-i-theoria organvm-ii-poiesis organvm-iii-techne organvm-iv-praxis organvm-v...`
- Bash: `ORGS=(4444J99 a-organvm meta-organvm organvm-i-theoria organvm-ii-poiesis organvm-iii-techne organvm-iv-praxis organvm-v...`
- Bash: `cat /tmp/ddm-slugs.txt | ~/.local/bin/dependabot-tier-classify --stdin > /tmp/ddm-classified.jsonl 2>/tmp/ddm-classify.e...`
- Bash: `REG=~/Code/organvm/organvm-corpvs-testamentvm/repo-registry.json
echo "=== registry top-level shape ==="
jq 'if type=="o...`
- Bash: `REG=~/Code/organvm/organvm-corpvs-testamentvm/repo-registry.json
echo "=== find the array/object that holds repo entries...`
- Bash: `REG=~/Code/organvm/organvm-corpvs-testamentvm/repo-registry.json
echo "=== entry for alchemical-synthesizer ==="
jq '.or...`
- Bash: `CLS=~/.local/bin/dependabot-tier-classify
echo "=== file type / size ==="; file "$CLS"; wc -l "$CLS"
echo "=== how it lo...`
- Bash: `sed -n '110,185p' ~/.local/bin/dependabot-tier-classify`
- Bash: `echo "=== REGISTRY_V2 env ==="; echo "REGISTRY_V2=${REGISTRY_V2:-<unset>}"
echo "=== resolve $REGISTRY_V2 if set ==="; [...`
- Bash: `python3 - <<'PY'
import importlib.machinery, importlib.util, os
path = os.path.expanduser("~/.local/bin/dependabot-tier-...`
- Bash: `ALL=/tmp/ddm-all.jsonl
# Bare repo slugs (unique), classify per-repo
jq -r '.repository.nameWithOwner' "$ALL" | sort -u ...`
- Bash: `ALL=/tmp/ddm-all.jsonl
# eligible repos = tier 2 or 3 AND protected=false
jq -r 'select((.tier==2 or .tier==3) and .prot...`
- Bash: `NOW=$(date -u +%s)
echo "now(UTC epoch)=$NOW  ($(date -u))"
: > /tmp/ddm-eval.jsonl
while IFS= read -r line; do
  slug=$...`
- Bash: `d=$(gh pr view 13 --repo a-organvm/alchemical-synthesizer --json url,number,isDraft,mergeable,mergeStateStatus,reviewDec...`
- Bash: `python3 - <<'PY'
import json, subprocess, time, sys
NOW = time.time()
cands = [json.loads(l) for l in open("/tmp/ddm-can...`
- Bash: `python3 - <<'PY'
import json
ev = [json.loads(l) for l in open("/tmp/ddm-eval.jsonl")]
elig = [r for r in ev if r.get("e...`
- Bash: `python3 - <<'PY'
import json, subprocess, time, sys
to_merge = json.load(open("/tmp/ddm-to-merge.json"))
def run(cmd, ti...`
- Bash: `echo "=== gh pr view author shape ==="
gh pr view 129 --repo a-organvm/classroom-rpg-aetheria --json author,mergeStateSt...`
- Bash: `python3 - <<'PY'
import json, subprocess, time
to_merge = json.load(open("/tmp/ddm-to-merge.json"))
def run(cmd, timeout...`
- Bash: `for pr in "a-organvm/classroom-rpg-aetheria 129" "a-organvm/my-block-warfare 20" "a-organvm/search-local--happy-hour 26"...`
- Bash: `for spec in "a-organvm/classroom-rpg-aetheria:129" "a-organvm/my-block-warfare:20" "a-organvm/search-local--happy-hour:2...`
- Bash: `python3 - <<'PY'
import json, time
from datetime import datetime
NOW=time.time()
allprs=[json.loads(l) for l in open("/t...`
- Bash: `~/.local/bin/scheduled-task-audit-bookend daily-dependabot-merge end "47-deps-tier2=28-tier3=1-merges=3-skipped=44" 2>&1...`

---

---

## Prompt Summary

**Total prompts:** 1
**Session duration:** ~11 min
**Avg time between prompts:** ~11.0 min

### Prompt Categories (heuristic)

- **Directives** (implement/build/create/add/write): 1
- **Questions**: 0
- **Fixes** (fix/error/bug/broken/fail): 1
- **Reviews** (check/verify/review/audit): 1
