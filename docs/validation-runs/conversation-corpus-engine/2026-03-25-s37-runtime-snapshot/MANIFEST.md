# Conversation Corpus Engine Runtime Snapshot

Date: 2026-03-25
Source repo: `~/Workspace/organvm-i-theoria/conversation-corpus-engine`
Source commit: `3fa116f`

## Purpose

This archive preserves the runtime surfaces that are intentionally ignored by the
engine repository itself:

- `reports/`
- `state/`
- `federation/`

The engine repo policy keeps those directories out of version control. This
snapshot exists so the exact local runtime manifestation from the S37 closeout
session also has a remote-tracked home.

## Archive

- Archive file: `conversation-corpus-engine-runtime-2026-03-25-s37.tar.gz`
- Size: `1361705 bytes`
- SHA-256: `17a201032198f9366bcc306694eff84d3a4dc06470d1a2fd0f6bf97b2a95799c`
- File listing: `CONTENTS.txt`

## Included Surface

- `reports/`: 121 files
- `state/`: 4 files
- `federation/`: 21 files
- Tar member count: 150 lines in `CONTENTS.txt` (includes directory entries)

## Notes

- This snapshot is evidence/archive material, not an authoritative live source.
- The authoritative code and test state for the same session is committed in the
  engine repo at `3fa116f`.
- The universal work-registry reconciliation for the same closeout is committed
  in the corpus repo history adjacent to this manifest.
