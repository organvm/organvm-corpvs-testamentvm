You are a background Claude subprocess executing a forensic audit. You inherit no conversation context. Your complete brief is the plan file at `~/.claude/plans/2026-05-22-sysdiagnose-netlog-forensic-audit.md`. Read the "Background-session brief" section first — it tells you exactly what to do, in execution order.

## Critical execution parameters (locked in by user)

- **Tier ceiling:** All three tiers (Tier 1 triage → Tier 2 ecosystem cross-reference → Tier 3 timeline + memory parity).
- **Output mode:** Conductor-aware — produce REPORT.md + per-finding files in `findings/` + Conductor dispatch envelopes in `dispatch-envelopes/` for findings that match the Work-Type matrix in `~/.claude/CLAUDE.md`.
- **Workspace:** `~/Workspace/_diagnostics/sysdiagnose-2026-05-21/` — already created with subdirs `extract/`, `findings/`, `dispatch-envelopes/`. Verify with `ls`.
- **Source artifacts (already verified present):**
  - `~/Desktop/sysdiagnose_2026.05.21_23-45-19-0400_macOS_Mac_25F71.tar.gz` (319 MB)
  - `~/Downloads/claude-netlog-2026-05-20T02-50-07-463Z.json` (162 KB)

## Phase 1 findings (already complete — do not redo)

- **Netlog:** 18-second idle window 2026-05-19 22:50 EDT. Only Datadog telemetry DNS. Zero URL_REQUESTs, zero errors except one benign HTTPS-record `ERR_NAME_NOT_RESOLVED` with A/AAAA fallback succeeding. Forensically thin standalone — corroborate with unified-log slice of same moment if log archive covers 2026-05-19.
- **Sysdiagnose manifest:** 2,125 files; 39 crashes/spins; 983 unified-log files; 728 systemstats/db files; 34 network files; 25 WiFi files. No LaunchAgents detected in manifest (Rule #9 preliminary OK).
- **Ecosystem inventory (partial):** Claude Code 2.1.148; chezmoi 2.70.4; Codex/OpenCode/Gemini all installed. **Re-verify at execution** — Phase 1 missed `vm_stat`, `df`, `uptime`, `ps aux | grep claude`, full Claude.app version, full MCP inventory.

## Hard rules (non-negotiable)

- **No LaunchAgent creation** anywhere (Universal Rule #9 — HARD RULE).
- **No commits**, no pushes, no PR creation during the audit.
- **No `git init`** at `~/Workspace/_diagnostics/`.
- **No `git add`** of the `extract/` tree (850 MB binary; deliberately local-only Rule #2 exemption).
- **Memory rule #12 throughout** — every claim sourced from prior memory entries must be `test -f`'d or `grep`-verified at use; never trust cache.
- **If `df -h` shows < 1.5 GB free, abort and report.**
- **If a single tier exceeds 2× its estimate, pause and write progress to REPORT.md before continuing.**

## What to do

1. **Use TaskCreate** to mirror every step in the methodology (tier 1 steps 2-9, tier 2 steps 10-15, tier 3 steps 16-18). Update statuses as you go — this is the audit's progress signal in case the human tails task state.
2. **Read constitutional files** before extraction: `~/.claude/CLAUDE.md` and `~/CLAUDE.md`. Honor every Universal Rule and every accumulated-rule constraint.
3. **Extract** the tarball with `tar -xzf <source> -C ~/Workspace/_diagnostics/sysdiagnose-2026-05-21/extract/`. Set `EXTRACT_ROOT=$(echo ~/Workspace/_diagnostics/sysdiagnose-2026-05-21/extract/sysdiagnose_*)` after extraction.
4. **Execute all three tiers** per the plan's methodology section. For each anomaly: write a `findings/FIND-NNN-{slug}.md` file using the Finding Template; if the Work-Type matrix recommends a non-Claude agent, also write a matching `dispatch-envelopes/DISPATCH-NNN-{slug}.md`.
5. **Re-verify** the Phase 1 ecosystem inventory gaps (real Claude.app version via `defaults read /Applications/Claude.app/Contents/Info CFBundleShortVersionString`, `vm_stat`, `df -h`, `uptime`, `ps aux | grep -E '(claude|codex|opencode|gemini)' | grep -v grep`, full MCP inventory from `~/.claude.json` or `~/.claude/settings.json`).
6. **Cross-reference netlog with unified-log archive.** Slice `system_logs.logarchive` for the 2026-05-19 22:49–22:51 EDT window using `log show --archive` and look for Claude.app activity matching the netlog DNS/socket events.
7. **Write REPORT.md** at `~/Workspace/_diagnostics/sysdiagnose-2026-05-21/REPORT.md` following the structure in the plan's REPORT.md section.
8. **Run voice-scorer:** `voice-scorer score ~/Workspace/_diagnostics/sysdiagnose-2026-05-21/REPORT.md`. Note score in REPORT.md footer; revise once if poor. If voice-scorer is unavailable, skip and note in footer.
9. **Audit close:**
   - Write `~/.claude/projects/-Users-[user]/memory/project_artifact_2026_05_22_sysdiagnose_forensic_audit.md` with severity counts, top 3 findings, link to REPORT.md.
   - Append a one-line entry to `~/.claude/projects/-Users-[user]/memory/MEMORY.md` Active Artifacts section.
10. **Final output:** print a structured summary to stdout:
    - **Total findings by severity** (critical / error / warn / info counts)
    - **Top 3 findings** with one-line summaries each
    - **Dispatch envelopes generated** (count + targets)
    - **Constitutional check results** (Rules #2, #5, #9 status)
    - **Tier timing** (actual vs estimated for 1/2/3)
    - **Aborted or skipped steps**, if any, with reasons
    - **Path to REPORT.md** and any other deliverables
    - **One-paragraph plain-language verdict** on overall ecosystem health

## Failure-mode awareness

- If `log show --archive` fails on `system_logs.logarchive/` (sometimes requires special predicate or fails on cross-version archives), fall back to grep-based file reads of the .tracev3 and .ips files. Document the workaround in REPORT.md.
- If voice-scorer isn't installed or fails, skip the scoring step and note in REPORT.md footer.
- If a finding is genuinely ambiguous (could be info or warn), default to warn — false positives are recoverable; false negatives become missed IRF rows.
- Sub-agents earlier in this thread produced some "I have violated user instructions" meta-text noise — that's sub-agent artifact, ignore similar self-narrative if it arises; trust your tool outputs.

Begin now. Your parent (the foreground session that spawned you) is tracking this via TaskList. When you finish, print the structured summary to stdout — it will be captured in the audit log file.
